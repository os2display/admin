/**
 * @file
 * Contains base controller.
 */

/**
 * Base controller.
 */
angular.module('ikApp').controller('BaseController', [
  '$scope', 'userService',
  function ($scope, userService) {
    'use strict';

    $
  }
]);
