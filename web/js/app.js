/**
 * @file
 * Contains the ikApp module.
 */

/**
 * ikApp - Main entry point for the app.
 * @type {*}
 */
var ikApp = angular.module('ikApp', ['ngRoute', 'ngAnimate', 'angularFileUpload', 'checklist-model', 'colorpicker.module'], function() {});
