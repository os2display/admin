parameters:
  modules:
    busModule:
      files:
        - /apps/busModule/busModule.js
      dependencies:

    logModule:
      files:
        - /apps/logModule/services/logService.js
      dependencies:
        - busModule

    mainModule:
      files:
        - /apps/mainModule/services/userFactory.js
        - /apps/mainModule/services/menuItemService.js
      dependencies:

  bootstrap:
    modules:
      - logModule
      - mainModule
    services:
      - logService
      - userFactory
      - menuItemService