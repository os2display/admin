/**
 * Created by turegjorup on 12/08/14.
 */

/*
describe('The Slide function', function() {

  beforeEach(function() {
    browser.driver.get('http://indholdskanalen.vm/');

    browser.driver.findElement(by.css('#username')).sendKeys('admin');
    browser.driver.findElement(by.css('#password')).sendKeys('admin');
    browser.driver.findElement(by.css('#_submit')).submit();

    brower.
  });

  afterEach(function() {
    browser.driver.get('http://indholdskanalen.vm/logout');
  });

  it('Should have correct title', function() {
    //expect(browser.findElement(by.css('.header')).isDisplayed()).toBeFalsy();
    expect(browser.getTitle()).toBe("Indholdskanalen");
  });


  describe('Navigation', function() {

    it('Should have hidden nav overlay', function() {
      expect(browser.findElement(by.css('.nav--overlay')).isDisplayed()).toBeFalsy();
    });

    it('Should have hidden menu', function() {
      expect(browser.findElement(by.css('.nav')).isDisplayed()).toBeFalsy();
    });

    it('Should show nav overlay on click', function() {
      browser.findElement(by.css('.header--nav-menu-link')).click();
      expect(browser.findElement(by.css('.nav--overlay')).isDisplayed()).toBeTruthy();
    });

    it('Should open menu on menu icon click', function() {
      browser.findElement(by.css('.header--nav-menu-link')).click();
      expect(browser.findElement(by.css('.nav')).isDisplayed()).toBeTruthy();
    });

    it('Should close menu on nav overlay click', function() {
      browser.findElement(by.css('.header--nav-menu-link')).click();
      //browser.findElement(by.css('.nav--overlay')).click();
      browser.actions().mouseMove({x: 50, y: 50}).click().perform();
      expect(browser.findElement(by.css('.nav')).isDisplayed()).toBeFalsy();
    });

  });


});
*/